<h1 class="text-center">Edit User</h1>

<div class="widget">
  <%= render WlWeb.Accounts.UserView , "_form.html",  Map.merge(assigns, %{changeset: @changeset, route_back: Routes.user_path(@conn, :show, @user.id), action_name: :update, action_to: Routes.user_path(@conn, :update, @user.id)}) %>
</div>
